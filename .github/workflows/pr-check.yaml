name: .net PR check

on:
	push:
		branches: ["master"]
	pull_request:
		branches: ["master"]

jobs:
	build-and-test:
		mame: Build and tests
		runs-on: ubuntu-latest
		steps:
    - uses: actions/checkout@v3